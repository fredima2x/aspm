import logging
import time
import json as js
import sys
from PyQt5.QtWidgets import (
    QApplication, QMainWindow, QDialog, QLabel,
    QVBoxLayout, QHBoxLayout, QSizePolicy,
    QListWidgetItem, QInputDialog, QMessageBox
)
from PyQt5.QtCore import Qt, QEvent, QTimer
from PyQt5 import uic

import lib.core.normals as normals
import lib.helper.CoreHelp as CoreHelp

class SearchWindow(QDialog):
    def __init__(self, connection):
        super().__init__()
        uic.loadUi(CoreHelp.resource_path("assets/search_user.ui"))
        self.conn = connection
        
        self.enter_button.clicked.connect(self.try_select)
        self.cancel_button.clicked.connect(self.try_cancel)
    def try_select():
        pass
    def try_cancel():
        pass
    
